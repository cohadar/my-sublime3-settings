{
	"cmd": ["chmod a+x '$file'"],
	"file_regex": "^[ ]*File \"(...*?)\", line ([0-9]*)",
	"selector": "source.shell",
	"variants":
	[
		{
			"name": "Run",
			"cmd": ["bash", "--login", "$file"]
		}
	]
}
